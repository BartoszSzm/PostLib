from PIL import Image
from PIL.ImageTk import PhotoImage
from tkinter import Label, Tk

root = Tk()

img_obj = Image.open('./images/main.jpg')
img_obj = img_obj.resize(size=(800, 200))
img = PhotoImage(img_obj)
mylabel = Label(root, image=img)
mylabel.grid(0)

    
root.mainloop()
